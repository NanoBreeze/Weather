<UserControl x:Name="WeatherPeek"
    x:Class="Weather.WeatherPeekIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Weather"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="278.313"
    d:DesignWidth="244.578">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image x:Name="image_Forecast" Grid.Row="0" Stretch="Fill" Source="{Binding ForecastImage, ElementName=WeatherPeek}" ></Image>

        <TextBlock x:Name="txtBlock_temperature"  Grid.Row="1" Text="{Binding Temperature, ElementName=WeatherPeek}" FontSize="100" Foreground="White"></TextBlock>

    </Grid>
</UserControl>
